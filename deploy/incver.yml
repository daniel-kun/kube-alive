apiVersion: apps/v1beta2
kind: Deployment
metadata:
  namespace: kube-alive
  name: incver-deployment
  labels:
    app: incver
spec:
  replicas: 1
  selector:
    matchLabels:
      app: incver
  template:
    metadata:
      namespace: kube-alive
      labels:
        app: incver
    spec:
      containers:
      - name: incver
        image: %%DOCKER_REPO%%/incver%%ARCHSUFFIX%%:v2
        env:
        - name: Foo
          value: Trigger3
        - name: DOCKER_REPO
          value: %%DOCKER_REPO%%
        volumeMounts:
        - name: docker-socket
          mountPath: /var/run/docker.sock
        securityContext:
          privileged: true
        ports:
        - containerPort: 8080
      volumes:
        - name: docker-socket
          hostPath:
            path: /var/run/docker.sock
            type: File
---
kind: Service
apiVersion: v1
metadata:
  namespace: kube-alive
  name: incver
spec:
  selector:
    app: incver
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
